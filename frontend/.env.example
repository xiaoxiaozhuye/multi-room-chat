# REST 地址可以是相对路径（开发环境由 Vite 代理至后端）。
VITE_API_BASE_URL=/api/v1
# 留空或使用相对地址时，开发环境将通过 Vite 代理转发，生产环境则使用当前站点。
VITE_WS_URL=/ws/v1/chat
# subprotocol 遵从 API 契约；仅旧版后端仍只能从 query string 读取令牌时才设置为 query。
VITE_WS_AUTH_MODE=subprotocol
